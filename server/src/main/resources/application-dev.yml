server:
  port: 2436

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://127.0.0.1:3306/notebook?useUnicode=true&characterEncodeing=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapping/*.xml
  type-aliiases-package: com.notebook.entity

devtools:
  livereload:
    enabled: true
    port: 9527
  restart:
    enabled: true

logging:
  level:
    com:
      notebook:
          log: debug